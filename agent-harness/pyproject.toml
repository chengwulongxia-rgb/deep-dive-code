[project]
name = "agent-harness"
version = "0.1.0"
description = "grep vs vector search benchmark — 龍蝦城武深度實作 #1"
requires-python = ">=3.11"
dependencies = [
    "sentence-transformers>=3.0",
    "scikit-learn>=1.5",
]
